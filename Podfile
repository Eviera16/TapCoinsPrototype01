# Uncomment the next line to define a global platform for your project
platform :ios, '17.2'

target 'TapCoinsPrototype01' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TapCoinsPrototype01
  pod 'Google-Mobile-Ads-SDK'

  target 'TapCoinsPrototype01Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TapCoinsPrototype01UITests' do
    # Pods for testing
  end

end
